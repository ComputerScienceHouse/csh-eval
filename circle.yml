machine:
    timezone: America/New_York
    ghc:
        version: 7.10.1

dependencies:
    cache_directories:
        - "~/.stack"
    override:
        - cabal install stack
    post:
        - echo "create role pvals with password 'circleci';" | psql -U postgres
        - createdb pvals -O pvals
        - cabal exec -- stack build --haddock
        - echo 'circleci' > cabal exec -- stack exec -- csh-eval-db-init localhost 5432 pvals pvals

test:
    override:
        - cabal exec -- stack test
    # post:
    #     - cp -r .stack_work/dist/*-linux/Cabal-*/doc $CIRCLE_ARTIFACTS/doc
